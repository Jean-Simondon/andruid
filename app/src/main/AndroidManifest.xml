<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.andruidteam.andruid">

    <application
        android:name=".app.AndruidApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.andruid">
        <activity android:name=".ui.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activity.CharacterActivity"
            android:label="@string/title_activity_main_pc"
            android:theme="@style/Theme.andruid.NoActionBar" />
        <activity
            android:name=".ui.activity.DungMasterActivity"
            android:label="@string/title_activity_main_dm"
            android:theme="@style/Theme.andruid.NoActionBar" />

    </application>

</manifest>